{
    "autoload": {
        "classmap": ["src/"]
    },
    "require": {
        "slim/slim": "^3.10",
        "monolog/monolog": "^1.23",
        "slim/php-view": "^2.2"
    },
    "scripts": {
        "build": [
            "@cs",
            "@test"
        ],
        "cs": "tools/php-cs-fixer fix . --diff --verbose",
        "test": "tools/phpunit"
    }
}
